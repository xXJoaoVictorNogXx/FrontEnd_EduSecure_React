// src/routes.tsx
import { createBrowserRouter } from "react-router-dom";
// Importe outras telas aqui futuramente (ex: Login, Configuracoes)

export const router = createBrowserRouter([
  // {
  //   path: "/login",
  //   element: <Login />,
  // }
]);
